spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bookstore
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.buaa.bookbook.model
server:
  port: 8080